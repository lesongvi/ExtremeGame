/*----------------------------------------------------------------------------*-
 | [Include] mcmd - MrDeath's Commands
 |  by MrDeath!
-*----------------------------------------------------------------------------*/

#include <a_samp>

/*----------------------------------------------------------------------------*/

#define CMD(%0,%1,%2,%3) \
		if (!strcmp(#%0, %1, true, (%2))) CMD:%0((%3), %1[(%2) + 1])

#define mcmd2(%0,%1,%2) \
		if (!strcmp(#%0, %1, true)) mcmd_%0((%2))



#define mcmd_init(%0) \
		format(%0, (128), "%s", %0[1])

/*----------------------------------------------------------------------------*/
